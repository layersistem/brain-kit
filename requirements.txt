# brain-kit - local embed/rerank dependencies (CPU, no LLM API). Needs Python >= 3.10.
# Recall (brain_bm25.py) is STDLIB ONLY and needs none of this - install with --no-embed to
# skip the lot. These are for brain_embed.py and brain_search.py: the first run downloads
# BGE-M3 (~2GB) plus the reranker from HuggingFace, after which everything is offline.
# Pins are LOWER BOUNDS on purpose: over-pinning to one machine's versions breaks fresh installs.
sentence-transformers>=2.7
torch>=2.2
transformers>=4.40
numpy>=1.24
huggingface-hub>=0.23
scikit-learn>=1.3
scipy>=1.10
tqdm>=4.66
